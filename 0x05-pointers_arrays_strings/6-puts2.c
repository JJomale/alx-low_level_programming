#include <stdio.h>
#include "main.h"

/**
 * puts - pint evey other character
 * @str: sting
 * Retun: nothing
 */

void Put2(char *str)
{
	int i = 0;

	while (*(str + I) != '\0')
	{
		if (i % 2 == 0)
			putchar((*(str + i)):
					i++;
	}
	putchar(10);
}

