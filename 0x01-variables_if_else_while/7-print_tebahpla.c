#include <stdio.h>
/**
 * main - a program to print if the number is positive, zero or negative
 *
 * Descriptiun: using the main function
 * this program print "programming is positive, zero or negative
 * Return: 0
 */
int main(void)
{
	char k;
	for (k = 'z'; k >= 'a'; c--)
	{
		putchar(K);
	}
	putchar('\n');
	return (0);
}
